---
description: Write a sift "use the harness" standing rule into the repo's CLAUDE.md (hard-law, idempotent)
---
Run `SIFT_REPO_ROOT="$(pwd)" bash "$SIFT_HOME/bin/sift" init-claudemd $ARGUMENTS` from the repo
root and report the result. (`$SIFT_HOME` = the sift-harness install path; see AGENTS.md.)
