---
description: Plan a sift packet (submitted → packeted; sets the scope-guard focus)
---
Run `SIFT_REPO_ROOT="$(pwd)" bash "$SIFT_HOME/bin/sift" plan $ARGUMENTS` from the repo
root and report the result. (`$SIFT_HOME` = the sift-harness install path; see AGENTS.md.)
