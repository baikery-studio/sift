---
description: Health-check the harness state (flags confirmed packets with a missing W3 artifact)
---
Run `SIFT_REPO_ROOT="$(pwd)" bash "$SIFT_HOME/bin/sift" doctor $ARGUMENTS` from the repo
root and report the result. (`$SIFT_HOME` = the sift-harness install path; see AGENTS.md.)
