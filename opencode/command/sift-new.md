---
description: Scaffold a new sift packet (sift packet new <id> [--profile toy|software|prose] [--paths a,b | --from-diff])
---
Run `SIFT_REPO_ROOT="$(pwd)" bash "$SIFT_HOME/bin/sift" new $ARGUMENTS` from the repo
root and report the result. (`$SIFT_HOME` = the sift-harness install path; see AGENTS.md.)
