---
description: Show the next actionable packet in a wave (compaction-survival resume)
---
Run `SIFT_REPO_ROOT="$(pwd)" bash "$SIFT_HOME/bin/sift" next $ARGUMENTS` from the repo
root and report the result. (`$SIFT_HOME` = the sift-harness install path; see AGENTS.md.)
